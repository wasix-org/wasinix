diff -ruN ra/crates/dbt-common/src/mod.rs rb/crates/dbt-common/src/mod.rs
--- ra/crates/dbt-common/src/mod.rs	2026-08-10 19:55:12.671462993 +0200
+++ rb/crates/dbt-common/src/mod.rs	2026-08-10 19:55:12.718453605 +0200
@@ -1,3 +1,4 @@
+#![cfg_attr(target_os = "wasi", feature(wasi_ext))]
 #[macro_use]
 pub mod macros;
 
diff -ruN ra/crates/dbt-common/src/stdfs.rs rb/crates/dbt-common/src/stdfs.rs
--- ra/crates/dbt-common/src/stdfs.rs	2026-08-10 19:55:12.671462993 +0200
+++ rb/crates/dbt-common/src/stdfs.rs	2026-08-10 19:55:12.718782895 +0200
@@ -150,7 +150,15 @@
 pub fn symlink<P: AsRef<Path>, Q: AsRef<Path>>(from: P, to: Q) -> FsResult<()> {
     let from = from.as_ref();
     let to = to.as_ref();
-    #[cfg(not(target_os = "windows"))]
+    #[cfg(target_os = "wasi")]
+    {
+        std::os::wasi::fs::symlink_path(from, to).lift(ectx!(
+            "Failed to create symlink from {} to {}",
+            from.display(),
+            to.display()
+        ))
+    }
+    #[cfg(all(not(target_os = "windows"), not(target_os = "wasi")))]
     {
         std::os::unix::fs::symlink(from, to).lift(ectx!(
             "Failed to create symlink from {} to {}",
diff -ruN ra/crates/dbt-common/src/tokiofs.rs rb/crates/dbt-common/src/tokiofs.rs
--- ra/crates/dbt-common/src/tokiofs.rs	2026-08-10 19:55:12.671462993 +0200
+++ rb/crates/dbt-common/src/tokiofs.rs	2026-08-10 19:55:12.719122958 +0200
@@ -106,6 +106,10 @@
 pub async fn symlink(target: impl AsRef<Path>, link: impl AsRef<Path>) -> FsResult<()> {
     let target = target.as_ref();
     let link = link.as_ref();
+    #[cfg(target_os = "wasi")]
+    {
+        crate::stdfs::symlink(target, link)
+    }
     #[cfg(unix)]
     {
         tokio::fs::symlink(target, link).await.lift(ectx!(
