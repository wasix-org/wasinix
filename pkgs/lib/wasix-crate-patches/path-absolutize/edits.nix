# Gates its unix path parser the way path-dedot does, on a feature its own
# manifest must enable; a dependent enabling path-dedot's does not reach it.
_: {
  edited = [">=3.1.1, <4.0.0"];
  stock = ["<3.1.1" ">=4.0.0"];
}
