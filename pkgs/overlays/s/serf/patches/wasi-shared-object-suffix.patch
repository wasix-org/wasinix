--- a/SConstruct
+++ b/SConstruct
@@ -301,6 +301,8 @@
 
 SOURCES = Glob('*.c') + Glob('buckets/*.c') + Glob('auth/*.c')
 
+env.Replace(SHOBJSUFFIX='.shared.o')
+
 lib_static = env.StaticLibrary(LIBNAMESTATIC, SOURCES)
 lib_shared = env.SharedLibrary(LIBNAME, SOURCES + SHARED_SOURCES)
 
