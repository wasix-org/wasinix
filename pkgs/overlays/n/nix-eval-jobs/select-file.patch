diff --git a/src/eval-args.cc b/src/eval-args.cc
--- a/src/eval-args.cc
+++ b/src/eval-args.cc
@@ -253,0 +254,15 @@ MyArgs::MyArgs() : MixCommonArgs("nix-eval-jobs") {
+
+    addFlag({
+        .longName = "select-file",
+        .aliases = {},
+        .shortName = 0,
+        .description =
+            "Read from a file a Nix function that transforms the evaluation "
+            "root. This is equivalent to --select without command-line "
+            "argument size limits.",
+        .category = "",
+        .labels = {"path"},
+        .handler = {&selectFile},
+        .completer = completePath,
+        .experimentalFeature = std::nullopt,
+    });
diff --git a/src/eval-args.hh b/src/eval-args.hh
--- a/src/eval-args.hh
+++ b/src/eval-args.hh
@@ -23,0 +24,1 @@ class MyArgs : virtual public nix::MixEvalArgs,
+    std::filesystem::path selectFile;
diff --git a/src/nix-eval-jobs.cc b/src/nix-eval-jobs.cc
--- a/src/nix-eval-jobs.cc
+++ b/src/nix-eval-jobs.cc
@@ -550,0 +551,5 @@ void validateIncompatibleFlags(const MyArgs &args) {
+    if (!args.selectExpr.empty() && !args.selectFile.empty()) {
+        throw nix::UsageError(
+            "--select and --select-file are mutually exclusive");
+    }
+
diff --git a/src/worker.cc b/src/worker.cc
--- a/src/worker.cc
+++ b/src/worker.cc
@@ -310,1 +310,4 @@ auto initializeRootValue(const nix::ref<nix::EvalState> &state,
-    if (args.selectExpr.empty()) {
+    const auto selectExpr = args.selectFile.empty()
+                                ? args.selectExpr
+                                : nix::readFile(args.selectFile);
+    if (selectExpr.empty()) {
@@ -315,2 +318,2 @@ auto initializeRootValue(const nix::ref<nix::EvalState> &state,
-    auto *selectExpr =
-        state->parseExprFromString(args.selectExpr, state->rootPath("."));
+    auto *parsedSelectExpr =
+        state->parseExprFromString(selectExpr, state->rootPath("."));
@@ -319,1 +322,1 @@ auto initializeRootValue(const nix::ref<nix::EvalState> &state,
-    state->eval(selectExpr, vSelect);
+    state->eval(parsedSelectExpr, vSelect);
